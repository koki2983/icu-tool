# ICUツール

ICU看護師がベッドサイドで使う、計算・スコア・知識の道具。
ブラウザで開くだけで動き、インストールもログインも不要。

**公開URL： https://koki2983.github.io/icu-tool/**

---

## 1. コンセプト

「ベッドサイドで、計算と確認と学び直しが3タップで終わる」道具。
狙いは計算の時短だけでなく、**知識と判断の標準化**。

- 測り方の標準化：スコアの判定基準と実施手順を選択肢そのものに埋め込み、誰が測っても同じ点になるようにする
- 伝え方の標準化：SBARの型、「先輩に聞かれること」形式で報告の言葉を揃える
- 施設ルールの標準化：自院の希釈・手順を「当院」カテゴリに置き、全員が同じ版を見る

患者情報は一切入力しない設計。入力したデータは端末内だけに保存され、外部に送信されない。

---

## 2. 中身（2026年9月時点）

| 項目 | 数 |
|---|---|
| 知識メモ | 63項目 |
| 用語集 | 316語 |
| 共通薬剤（希釈プリセット） | 14 |
| 自作の図（SVG） | 72点 |
| 写真・解剖イラスト | 44点 |
| ファイルサイズ | 約3.4MB |

**タブ構成**

- 計算（ジャンル別タブ：呼吸・薬剤・循環・電解質・栄養）：P/F比＋酸素早見表／RSBI／ROX／予測体重と1回換気量／血液ガスの代償判定／ガンマ計算／薬剤別の投与量の目安／配合変化を調べる（2剤判定）／血行動態／輸血の予測Hb／電解質の補正／メイロン／FENa／栄養／ハリス・ベネディクト／不感蒸泄
- 輸液：輸液シートのIN/OUT・バランス計算（下の「輸液タブ」参照）
- 薬剤：共通希釈（削除不可、一般名と商品名を併記）＋自分で登録する希釈（投与量の目安欄つき）
- スコア：GCS／JCS／RASS／CPOT／Child-Pugh／NIHSS（実施方法つき）／OHAT（説明つき）
- 知識：カテゴリ別（当院・呼吸・循環・補助療法・術後・神経鎮静・薬剤感染・全身管理・解剖・基準値）。本文内リンク・関連項目・目次・戻るボタン・「AIに聞く」
- 用語：検索つき用語集

### 輸液タブ（輸液シートの計算補助）

紙の輸液シートの **IN／OUT／バランスの計算だけ** を補助する。記録と確認は紙で行う。

- 患者1〜3を切り替えて入力（区別のメモは「3床」など。**氏名は入れない**）。患者ごとに色が変わる
- 点滴：選択リストから選ぶ（手入力も可）。「流量（mL/h）」か「投与量と時間」で入力。流量変更・OFFを時刻つきで追加
- 単回IN：側管iv、内服の水分、食事、飲水、経管栄養、回収生食（CHDF・HD）、輸血（RCC・FFP・PC・5%/25%アルブミン・回収血）
- OUT：尿、胃管、排便、除水、嘔吐、ドレーン（名前はタブ＋手入力）、出血。時刻はワンタップ（13:59・17:59…）
- 区分：胃管は水分、ドレーンはすべて血液バランスに入る
- 集計：4時間バランス（14・18・22・2・6・10時）か8時間バランス（18・2・10時）を患者ごとに選ぶ。「区間／累計」で表示。区間は「始まりの時刻から次の区切りの1分前まで」（13:59は10〜14時、14:00は14〜18時）
- 体重を入れると区間ごとの尿量をmL/kg/hで表示（0.5未満は赤）
- 10時になると「新しいシート」の案内が出る。続いている点滴を持ち越すか選べる。前のシートは翌日10時に自動削除
- 「1つ戻す」「この患者をクリア」「全員分をクリア」
- データは端末内（localStorage）のみ。外部に送信しない

---

## 3. ファイル構成

**`index.html` の1ファイルのみ。** HTML・CSS・JavaScript・画像（base64）がすべてこの中に入っている。

- ビルド不要。エディタで開いて編集し、ブラウザで開けばそのまま動く
- 写真44枚は `img/` フォルダに分離（起動を軽くするため）。本体と同じサーバーから読み込むので圏外では写真だけ表示されない
- 外部サーバーへの通信なし（AIに聞く機能は、外部サービスをブラウザで開くだけ）
- リポジトリには `.nojekyll` を置いている（GitHub Pagesを静的配信にして公開を安定させるため。消さない）
- データ保存は端末内のみ（localStorage）。メモ・写真・自分の薬剤は端末ごとに独立

### 中の構造（JavaScript内の主な変数）

| 変数 | 中身 |
|---|---|
| `PRESET` | 共通薬剤の希釈。名前・mg・mL・単位・目安（rt/lo/hi）・注意（rn） |
| `KNOW` | 知識メモの本体。`{t:タイトル, fig:図, s:[{h:見出し, b:[本文]}]}` |
| `META` | 各項目のカテゴリ・「まず押さえる」3点・図の配置 |
| `FIG` | 自作のSVG図。文字データなので容量は軽い |
| `PHOTO` / `PHOTOCRED` | 写真・イラスト（base64）と出典 |
| `GLOSS` | 用語集。`[用語, 別名(|区切り), 説明, リンク先項目]` |
| `CATS` / `ORDER` | カテゴリと表示順 |
| `XLINK` | 本文中の自動リンクのキーワード |

---

## 4. 更新のしかた

### A. Claudeに頼む（現在の運用）

1. Claudeに変更内容を伝える
2. Claudeが `index.html` を編集し、GitHub APIで直接反映
3. 数分で公開URLに反映される

**トークン**
- classic personal access token（権限は `public_repo` のみ、**無期限**）。https://github.com/settings/tokens に表示される
- トークン本体は管理者のロック付きメモに保管。**リポジトリやREADMEには絶対に書かない**（公開されると自動で無効化される）
- 新しいチャットでClaudeに更新を頼むときは、このトークンを貼る
- 漏えいが疑われたら上記ページでDelete → 同じ設定で作り直す

### B. 手作業でアップロード

1. リポジトリ https://github.com/koki2983/icu-tool を開く
2. 「Add file」→「Upload files」で `index.html` を選ぶ（同名なので上書きされる）
3. 「Commit changes」

### C. Mac（Claude Code など）から

```bash
git clone https://github.com/koki2983/icu-tool.git
cd icu-tool
# index.html を編集
git add index.html && git commit -m "変更内容" && git push
```

### 反映の確認

GitHub Pagesは10分ほどキャッシュが残る。すぐ見たいときは末尾に番号を付ける。

```
https://koki2983.github.io/icu-tool/?v=42
```

配布するときは番号なしのURLでよい。

---

## 5. 編集のときの約束ごと

### 医療情報

- 数値は一般的な目安として書き、**施設基準・医師の指示が優先**であることを明記する
- 自院だけのルールは「当院」カテゴリに置くか、本文に「当院は〜」と書いて出所を分かるようにする
- ガイドラインを引くときは年号を入れる（例：J-SSCG 2024）
- 書籍・資料の文章や図をそのまま写さない。内容は自分の言葉で書き直し、図は描き起こす

### 画像

- 公開ライセンス（CC0・パブリックドメイン・CC BY・CC BY-SA）のものだけを使う
- 作者名・ライセンス・元画像へのリンクを画像の下に必ず表示する（`PHOTOCRED`）
- 患者の画像を使う場合は、施設の規定と同意の確認が必要。氏名・ID・日付は必ず外す

### 表記

- 数値には必ず単位をつける（mEq/L、mmHg、mg/dL、γ など）
- 略語は初出で日本語を添える。馴染みのない言葉（burden など）は言い換えを併記する
- 薬剤は一般名と商品名を併記する
- 「ペーシングフェラー」「センシングフェラー」のように、現場で使う言い方に合わせる

### 図（SVG）

- 文字がviewBoxの外に出ると切れる。編集したら必ずスマホ幅（390px）で表示を確認する
- 1行あたりの目安：font-size 9 で全角約30文字

---

## 6. 困ったとき

| 症状 | 対処 |
|---|---|
| 更新したのに変わらない | URLの末尾に `?v=数字` を付けて開く。ホーム画面アプリは一度完全に閉じる |
| 公開がなかなか反映されない | GitHubの Actions タブで「pages build and deployment」を確認。止まっていれば数分待つか、再実行する |
| 画面が真っ白 | JavaScriptの構文エラー。GitHubの「History」から前のバージョンに戻す |
| 前の状態に戻したい | リポジトリ → History → 戻したいコミット → 「Browse files」→ そのバージョンのindex.htmlをダウンロードして再アップロード |
| 重い・遅い | 画像の圧縮率を上げる、または大きい画像の幅を縮める。目安は6〜7MBを超えたら |
| メモが消えた | 端末内保存のため、ブラウザのデータ削除で消える。ホーム画面に追加した状態での使用を周知する |

---

## 7. 今後の課題

- 容量：現在3.3MB。写真を増やすなら圧縮か別ファイル化を検討
- 内容の見直し：ガイドライン改訂時（J-SSCG、PADIS、ARDS定義など）に該当項目を更新
- 「当院」カテゴリの内容は、病棟の手順書が改訂されたら合わせて直す

---

## 8. 出典

知識メモの構成は『3年目ICUナースのノート 改訂増強版』（道又元裕 総監修、日総研出版）のテーマを骨組みにしているが、本文はすべて独自にまとめたもの。
参照したガイドラインはアプリ内「出典・参考ガイドライン」に一覧がある。

画像は Wikimedia Commons の公開ライセンス作品（OpenStax、Blausen Medical、BodyParts3D ほか）。各画像の下に出典を表示している。

---

作成・運用：ICU（熊本労災病院）
最終更新：2026年9月25日（疾患15項目、計算ツールのジャンル分け、写真を img/ に分離）
